Quick client scaffold (Vite + React)

Install and run locally:

```bash
cd client
npm install
npm run dev
```

Open the app at http://localhost:5173 and use the login form to authenticate against the backend.

Environment:
- To change the backend URL, set `REACT_APP_API_URL` in your environment before running Vite.

