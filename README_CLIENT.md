Quick client scaffold (Vite + React)

Install and run locally:

```bash
cd client
npm install
npm run dev
```

Open the app at http://localhost:5173 and use the login form to POST to `http://localhost:4000/auth/login`.
